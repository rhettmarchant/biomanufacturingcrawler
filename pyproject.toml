[project]
name = "biomfg-crawler"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pydantic>=2.9.7", "python-dotenv>=1.0.0"]

"pydantic>=2.9.7", "python-dotenv>=1.0.0"

[build-system]
requires = ["setuptools>=68","wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["biomfg_crawler"]
