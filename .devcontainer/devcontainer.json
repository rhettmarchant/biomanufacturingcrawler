{
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker",
        "ms-python.python",
        "alexkrechik.cucumberautocomplete",
        "mechatroner.rainbow-csv",
        "grapecity.gc-excelviewer",
        "jimasp.behave-vsc"
      ]
    }
  },
  "mounts": [
    "source=/home/rhett/.ssh,target=/home/vscode/.ssh,type=bind,consistency=cached"
  ],
  "postCreateCommand": "pip install -r requirements.txt; bash auth_github.sh"
}